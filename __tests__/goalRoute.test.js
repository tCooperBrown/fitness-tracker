// To Complete
// This test suite is skipped in Jest config as xdescribe still causes tests to hang.

xdescribe("Modifying a user's active goal", () => {
  test("creating a new goal", () => {});
  test("updating an existing goal", () => {});
  test("deleting an existing goal", () => {});
});
